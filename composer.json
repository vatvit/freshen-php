{
  "name": "vatvit/freshen-php",
  "description": "Stale-while-revalidate cache with stampede prevention (single-flight recompute + jittered TTLs).",
  "type": "library",
  "license": "MIT",
  "keywords": [
    "cache",
    "stale-while-revalidate",
    "swr",
    "stampede",
    "redis",
    "psr-6",
    "psr-16"
  ],
  "require": {
    "php": ">=8.1",
    "psr/event-dispatcher": "^1.0",
    "psr/log": "^3.0",
    "tedivm/stash": "v1.2.1"
  },
  "suggest": {
    "ext-redis": "To use the Redis-backed driver (Freshen\\Driver\\Redis)."
  },
  "require-dev": {
    "phpunit/phpunit": "^10.5 || ^11.0",
    "phpstan/phpstan": "^1.11"
  },
  "autoload": {
    "psr-4": {
      "Freshen\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Freshen\\Tests\\": "tests/"
    }
  },
  "scripts": {
    "test": "phpunit",
    "test:integration": "phpunit --testsuite integration",
    "analyse": "phpstan analyse",
    "coverage": "phpunit --coverage-clover build/clover.xml",
    "coverage:gate": [
      "@coverage",
      "@php ../../scripts/php-coverage-gate.php build/clover.xml 90"
    ]
  },
  "scripts-descriptions": {
    "test": "Run the unit suite (deterministic, no live backends).",
    "test:integration": "Run the live-Redis lane; needs ext-redis + a Redis (see scripts/php-redis-it.sh).",
    "analyse": "PHPStan level max.",
    "coverage": "Write a Clover report to build/clover.xml (needs a coverage driver, e.g. PCOV).",
    "coverage:gate": "Measure coverage and enforce the floor, excluding the Redis driver."
  },
  "config": {
    "sort-packages": true
  },
  "minimum-stability": "stable",
  "prefer-stable": true,
  "homepage": "https://github.com/vatvit/freshen",
  "support": {
    "issues": "https://github.com/vatvit/freshen/issues",
    "source": "https://github.com/vatvit/freshen/tree/main/packages/php"
  }
}
